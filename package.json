{
  "name": "ahp-workbench",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check-app.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
